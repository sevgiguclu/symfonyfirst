{% extends 'base.html.twig' %}

{% block title %}Advertisement{% endblock %}

{% block body %}
    <h1>Advertisement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advertisement.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ advertisement.title }}</td>
            </tr>
            <tr>
                <th>Carid</th>
                <td>{{ advertisement.carid }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ advertisement.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ advertisement.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ advertisement.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ advertisement.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_advertisement_index') }}">back to list</a>

    <a href="{{ path('admin_advertisement_edit', {'id': advertisement.id}) }}">edit</a>

    {{ include('admin/advertisement/_delete_form.html.twig') }}
{% endblock %}
