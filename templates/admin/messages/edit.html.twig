{% extends 'adminbase.html.twig' %}

{% block title %}Edit Car{% endblock %}

{% block body %}
    <!-- ============================================================== -->
    <!-- wrapper  -->
    <!-- ============================================================== -->
    <div class="dashboard-wrapper">
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                <h3 class="text-center">
                    <h1>Edit Messages</h1>
                    <a href="{{ path('admin_messages_index') }}" class="btn btn-rounded btn-dark">back to list</a>
                    {{ include('admin/messages/_form.html.twig', {'button_label': 'Update'}) }}


                    {{ include('admin/messages/_delete_form.html.twig') }}
                </h3>
            </div>
        </div>
    </div>
    <!-- ============================================================== -->
{% endblock %}